# 额外禁止项 — apps/web

> 继承根 [`../../../rules/forbidden.md`](../../../rules/forbidden.md) 全部条款，以下为 apps/web 额外约束（只增不减）。

- 禁止在前端硬编码密钥 / token / 第三方私钥。
- 禁止绕过统一请求封装直接发起跨子项目调用（须经既定契约层）。
- 禁止在前端实现本应由后端把关的安全校验（如仅靠前端隐藏来做权限控制）。

<!-- TODO（项目接入时可追加）。 -->
