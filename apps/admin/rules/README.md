# apps/admin 本地规则

> 占位。请参照 [`../../web/rules/`](../../web/rules/) 的范例，按需建立有本地增量的文件：
> `scope.md`、`high-impact.md`、`forbidden.md`、`development-guard.md`（栈特有检查）。
>
> 通用规则继承自根 [`../../../rules/`](../../../rules/)，本地只增不减；无本地增量的规则（如 review-gates）直接继承根，不必建。
