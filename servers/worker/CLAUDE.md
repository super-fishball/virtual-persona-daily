# CLAUDE.md — servers/worker

> 本子项目结构与 `servers/api` 同构。**完整范例见 [`../api/`](../api/)**，按其模式填充本目录。

## 1. 子项目说明

`servers/worker` 是后台异步任务 / 队列消费服务。

<!-- TODO（项目接入时填写）：职责与定位。 -->

## 待补充

参照 [`../api/CLAUDE.md`](../api/CLAUDE.md) 的结构补全：必读上下文、必读规则、技术栈、常用命令、读取顺序、边界。

## 本地规则

参照 [`../api/rules/`](../api/rules/) 建立本目录 `rules/`。
