# 可修改范围与边界 — servers/api

## 可改

- `servers/api/` 内的源码、迁移、后端测试、本服务文档。

## 不可改（跨边界）

- 任何 `apps/*`、其他 `servers/*`、根 `rules/`、根 `context/`。
- **既有的对外契约**（已被 `apps/*` 依赖的接口）——不得单方变更，须经 feature 层集体定稿。

## 越界处理

发现任务需要改动 `servers/api` 之外、或改动既有契约 → **暂停**，走 feature 层或转交对应负责人。

<!-- TODO（项目接入时可细化）。 -->
