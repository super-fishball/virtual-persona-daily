# .claude/task.env.example —— 「按任务注入」hook 变量模板（committed）。
#
# 用法：开一个子项目任务时，复制为 .claude/task.env（gitignored）并填值。
#   cp .claude/task.env.example .claude/task.env
# 两个 hook（path-guard.sh / check-completion.sh）启动时 source 它，改文件即生效，无需重启会话。
#
# TASK_SCOPE：本任务允许改动的单一子项目（相对仓库根）。空 = 不查越界。
#   越界（改 TASK_SCOPE 之外的 apps/*、servers/*）→ path-guard exit 2 挡停。
#   例：servers/api ｜ apps/web ｜ servers/generation-service ｜ servers/ai-gateway
#
# TEST_CMD：结束前必须通过的测试命令。Stop hook 会跑它，非 0 → block 挡停续做。空 = 不卡测试。
#   例（api）：    cd servers/api && pnpm test
#   例（web）：    cd apps/web && pnpm test
#   例（gen）：    make -C servers/generation-service test
#   例（gateway）：make -C servers/ai-gateway test
#
# ⚠️ Phase 0 无活动子项目任务 → 两者都留空（或不建 .claude/task.env）。
TASK_SCOPE=
TEST_CMD=
